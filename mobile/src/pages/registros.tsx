import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export function registros({ navigation }) {

  return (
      <View>
              <Text>Registros</Text>
      </View>
  );
}
const styles = StyleSheet.create({
});