import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export function manutencao({ navigation }) {

  return (
      <View>
              <Text>Manutencao</Text>
      </View>
  );
}
const styles = StyleSheet.create({
});