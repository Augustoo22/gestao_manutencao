import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export function solicitacoes({ navigation }) {

  return (
      <View>
              <Text>Solicitações</Text>
      </View>
  );
}
const styles = StyleSheet.create({
});