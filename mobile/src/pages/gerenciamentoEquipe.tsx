import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export function gerenciamentoEquipe({ navigation }) {

  return (
      <View>
              <Text>Gerenciamento Equipe</Text>
      </View>
  );
}
const styles = StyleSheet.create({
});